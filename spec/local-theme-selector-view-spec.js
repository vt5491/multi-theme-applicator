// Generated by CoffeeScript 1.10.0
var LocalThemeManager, LocalThemeSelectorView, MultiThemeApplicator;

LocalThemeSelectorView = require('../lib/local-theme-selector-view');

MultiThemeApplicator = require('../lib/multi-theme-applicator');

LocalThemeManager = require('../lib/local-theme-manager');

describe('LocalThemeSelectorView', function() {
  ({
    localThemeSelectorView: null,
    activationPromise: null
  });
  beforeEach(function() {
    this.activationPromise = atom.packages.activatePackage('multi-theme-applicator');
    return this.localThemeSelectorView = new LocalThemeSelectorView();
  });
  it('ctor works', function() {
    expect(this.localThemeSelectorView.localThemeManager).toBeDefined();
    return expect(this.localThemeSelectorView.localThemeManager).toBeInstanceOf(LocalThemeManager);
  });
  it('doIt works', function() {
    return expect(this.localThemeSelectorView.doIt()).toEqual(7);
  });
  return it('getCurrentGlobalSyntaxTheme works', function() {
    var currentTheme;
    return currentTheme = this.localThemeSelectorView.getCurrentGlobalSyntaxTheme();
  });
});

//# sourceMappingURL=local-theme-selector-view-spec.js.map
