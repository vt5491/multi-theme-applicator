'use babel';

import ThemeSelectorView from '../lib/theme-selector-view';

console.log('vt: now in spec');
jasmine.log('vt: now in spec');
describe('ThemeSelectorView', () => {
  console.log('vt: now in spec 2');
  jasmine.log('vt: now in spec 2');
  it('has one valid test', () => {
    //expect('life').toBe('easy');
    expect(7).toEqual(7);
  });
});
