// Generated by CoffeeScript 1.10.0
var $, LocalStylesElement, jQuery;

LocalStylesElement = require('../lib/local-styles-element');

$ = jQuery = require('jquery');

describe('LocalStylesElement', function() {
  ({
    localStylesElement: null
  });
  beforeEach(function() {
    return this.localStylesElement = new LocalStylesElement();
  });
  it('doIt works', function() {
    return expect(this.localStylesElement.doIt()).toEqual(7);
  });
  return it('createStyleElement works', function() {
    var $result, css, cssRegexSafe, re, result, sourcePath;
    css = "atom-text-editor, :host {\n  background-color: #e3d5c1;\n  color: #000000;";
    cssRegexSafe = css.replace(/\{/, '\\{');
    sourcePath = '/tmp/local-styles-element/index.less';
    result = this.localStylesElement.createStyleElement(css, sourcePath);
    $result = $(result);
    console.log('LocalStylesElement.createStyleElement: result=' + result);
    expect(result).toBeDefined();
    expect(result).toBeInstanceOf(HTMLElement);
    expect($result.attr('source-path')).toEqual(sourcePath);
    expect($result.attr('context')).toEqual('atom-text-editor');
    expect($result.attr('priority')).toEqual('1');
    re = new RegExp(cssRegexSafe, "m");
    return expect($result.text().match(re)).not.toBeNull();
  });
});

//# sourceMappingURL=local-styles-element-spec.js.map
